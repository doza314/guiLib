namespace guiLib
{
    public enum ButtonState
    {
        Idle,
        Active,
        Off,
        On
    }

    public enum machineState
    {
        Off,
        On
    }

    public enum action
    {
        open,
        close,
        pause,
        resume
    }
    
    
}