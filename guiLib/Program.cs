
using SFML.Graphics;
using SFML.System;
using SFML.Window;

//var window = new RenderWindow(new VideoMode(new Vector2u(800, 600)), "Test");
/*while (window.IsOpen)
{
    window.DispatchEvents();
    window.Clear(Color.Black);
    window.Display();
}
*/

